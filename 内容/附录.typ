#import "../../模板/lib.typ": *


= #设定元素[不对称婚姻]

#元素[怪动植物]与#元素[嗜血仙子]之间一种*一夫一妻制与奴隶制相结合的婚姻制度*,也是男性#元素[怪动植物]成年_升入大学_的*唯一途径*;_其生殖基础见_

#TODO[#元素[不对称结婚]的具体设定]

= #设定元素[不对称生殖]

= #设定元素[计划生育]

= 排版规范

本文档在撰写过程中遵循以下排版约定,便于后续编辑和协作。

== 排版

两段之间空一行

字之间别随便空格

== 标点符号

所有中文标点统一替换为半角符号+空格:

#strike[但是排版符号前后不要随便空格]

#表格("标点符号规范", columns: 3,
  table.header([*全角_原始_*], [*半角_替换_*], [*说明*]),
  [`""`], [`""`], [中文引号替换为半角双引号],
  [`''`], [`''`], [中文单引号替换为半角单引号],
  [`，`], [`,`], [全角逗号替换为半角逗号],
  [`：`], [`:`], [全角冒号替换为半角冒号],
  [`；`], [`;`], [全角分号替换为半角分号],
  [`、`], [`,`], [顿号替换为半角逗号],
  [`——`], [`--`], [全角破折号替换为两个半角连字符],
  [`（`], [`(`], [全角左括号替换为半角左括号],
  [`）`], [`)`], [全角右括号替换为半角右括号],
)

== 括注内容

原文中用括号 `()` 括起的补充说明内容,在排版时:

#提示框("括注处理规则", [
  - 移除括号本身
  - 括号内的内容改为*斜体*
  - 示例: `*#元素[地狱]*(海拔约 12000m)` → `*#元素[地狱]*_海拔约 12000m_`
])

== 大数

万级及以上大数改用*科学计数法*:

#提示框("大数处理规则", [
  - 万级及以上数值改用科学计数法
  - 示例: `3万m` → `$3 times 10^4$ m`,`2万km` → `$2 times 10^4$ km`
  - 万级以下数值把0展开写
])

== 引用

文档内交叉引用使用*自动编号引用*形式:

#提示框("引用规范", [
  - 目标标题后添加*标签*: `= 标题 <label>`
  - 引用处使用*自动引用* `@label`,自动显示*编号与标题*,并生成可点击链接
  - 示例: `= 不对称生殖 <不对称生殖>` → `详见 @不对称生殖`
  - 不使用手动文本引用_如"见 xxx 一节"_或自定义链接,以保证编号一致
])

== 称呼

我没把这里面的任何生物当人,所以一律用它

== 单位

温度用K
